import 'package:flutter/material.dart';

class AppSubheader extends StatelessWidget {
  const AppSubheader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
